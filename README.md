# quiteer

```json
// package.json
{
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  }
}
```

npm adduser

npm login

npm logout

npm publish

# 撤回
npm unpublish [pkg]@[version]
