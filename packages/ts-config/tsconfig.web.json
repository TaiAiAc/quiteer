{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "lib": [
      "ES2016",
      "DOM",
      "DOM.Iterable"
      // No `ScriptHost` because Vue 3 dropped support for IE
    ],
    "types": []
  }
}
