{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "lib": [
      "ESNext",
      "DOM",
      "DOM.Iterable"
      // No `ScriptHost` because Vue 3 dropped support for IE
    ],
    "types": []
  }
}
